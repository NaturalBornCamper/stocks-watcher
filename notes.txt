TODO:

MAYBE A GOOD IDEA:
-Replace API Calls with Google Sheets API, which can get adjusted close prices
 https://chatgpt.com/c/681989f2-167c-800e-b5b8-906976b89dc8 for idea how to do it, essentially this:
1. Create the Google Sheet
 Create a tab (e.g., Prices)
 Use GOOGLEFINANCE("TICKER", "close", DATE(...)) to pull historical data
 Optionally use =TODAY() to automate "yesterday's" or "latest close"
2. Build an Apps Script Web App
 Open the sheet → Extensions > Apps Script
 Write a script to:
 Accept GET requests with ticker and date
 Return the adjusted close price from the sheet
3. Deploy the Script as a Web App
 Under Apps Script > Deploy > “Manage Deployments”
 Set access to “Anyone” (or restricted to your Google account)
 Note the URL — this becomes your new API endpoint
4. Update Your Python App
 Replace old API calls with a requests.get() to your Google Sheet API
 Parse the JSON or text it returns
 Log or store results as before
Bonus (Optional Enhancements)
 Add caching or retry logic if Google is slow or flaky
 Log all fetched values for debugging
 Build a bulk endpoint (e.g., return prices for 10 tickers at once)


-PRIORITIZE
-Add sort order in url when clicking it (query param), along with all future filters with checkboxes, so that changing date or category re-sorts the same
-Make missing run configurations and just like game library, make them for VS code too
 check if Claude.md specifies run configs should be added for both
 Link run configs from Games library for inspiration
-Urgent Quant issue: Symbol or name change, or merge with other company breaks history. Find a solution, maybe ask AI for ideas
 Symbol changes in data dumps causes issues. For example when a top stock (SCZMF) changed to SCZMD, the system thought it was 2 different stocks so the score was reset.
-Make the other quant views
 https://docs.djangoproject.com/en/5.0/topics/http/urls/#passing-extra-options-to-view-functions
 Add checkbox to hide gated stocks
 Quant page for a month, so I can basically see the same data as Excel, but split it different tables on the same page instead of one long table
  show all category, with checkboxes to hide/show them
 Average "quant" column of the Quant table for each stock instead of my own custom score
 Quant page for a certain stock, that will show its rank for each month, so I can have the details for a stock
  Showing the history of each type of quant it was in
  showing a chart
 Quant page for a specific category. Not sure how the data will show on the page, TBD
 Page for average of each category.
  For example top technology, all stocks that were in at least once have the total time they were in, the average of each column, their calculated rank
-Quant views changes
 Menu to open each quant view
 Filter to show minimum score/count in at least one of the types to show a stock in the list. Right now there are 3313.. too much
  Min score 50 by default I think. I can always show all with a button
 Filter/search for stock
 (All these filters and options will be different according to the quant view also)
-Alert system to send email when a stock that I "favorited" (I have in my portfolio) has been gated out
-To speed page sorting up, have checkbox to only show stocks with a score of at least X (50?) in any column


EASY AND QUICK:
-Cronjob once per month to import data dump (Do it every hour on the first of the month)


-Add Zacks monthly top 5. Or top 10? Also free 30 days


-Quant auto-grabber
 Should be able to grab all URLs in a list, then copy the first 100 rows, then output in a csv
 Might need to be in Javascript, like a chrome extension that will open all links and generate


-Alerts rework
 Problem = need alerts with different columns
  Alerts when price hits above or less X$
  Alerts when price changes +/- X% since last day (Would be nice if a single alert can be only one direction or both)
  Alerts when price is highest/lowest in minimum X days (Would be nice if a single alert can be only one direction or both)
 Solutions
  Make a main alert table, with a sub table for each alert type?


-Quant Data structure possible rework
 Google sheets
  A sheet per quant type?
   +Maximum amount of sheets
   +No need to have a column for quant type
   -Big reorganization, might need a script to reorganize
 Django tables (Not sure it's needed, or that it's a good idea. I feel it will make queries more difficult)
   A table per quant type? If I choose to do this, they can all be moved in there later
   A table per month? Nope
   A table where all are mixed? Already like this


-Log system
 Make log function like Swarm Control, with level
 Log all prints better in function